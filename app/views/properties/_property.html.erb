<tr>
  <th><%= full_address(property) %></th>
  <th><%= "#{meter_display(property.meter)} kWh" %></th>
  <th><%= link_to "Manage", customer_property_path(@customer, property),:role => 'button', :class => "btn btn-success btn-sm"  %></th>
</tr>